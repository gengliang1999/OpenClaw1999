"/**\n * 聊天页面\n * 功能：对话列表管理、消息收发、Markdown 渲染、流式打字机效果、模型切换\n */\n\n// 示例对话数据\nlet conversations = [\n  { id: 'conv-1', title: '关于 JavaScript 闭包', time: '今天 14:30', messages: [\n    { role: 'user', content: '请解释一下 JavaScript 中的闭包是什么？' },\n    { role: 'ai', content: '**闭包（Closure）**是 JavaScript 中一个非常重要的概念。\\n\\n简单来说，闭包是指一个函数能够**记住并访问**它的词法作用域，即使这个函数在其词法作用域之外执行。\\n\\n```javascript\\nfunction outer() {\\n  let count = 0;\\n  return function inner() {\\n    count++;\\n    return count;\\n  };\\n}\\n\\nconst counter = outer();\\nconsole.log(counter()); // 1\\nconsole.log(counter()); // 2\\n```\\n\\n在这个例子中，`inner` 函数就是一个闭包，它可以访问 `outer` 函数中的 `count` 变量。' },\n  ]},\n  { id: 'conv-2', title: 'Python 数据分析', time: '昨天 09:15', messages: [\n    { role: 'user', content: '用 Python 做数据分析有哪些常用库？' },\n    { role: 'ai', content: 'Python 数据分析常用库：\\n\\n1. **Pandas** — 数据操作和分析\\n2. **NumPy** — 数值计算\\n3. **Matplotlib** — 数据可视化\\n4. **Seaborn** — 统计图表\\n5. **Scikit-learn** — 机器学习\\n6. **SciPy** — 科学计算' },\n  ]},\n];\n\n// 当前活跃对话\nlet activeConvId = 'conv-1';\n// 是否正在生成回复\nlet isGenerating = false;\n// 可选模型列表\nconst MODELS = ['GPT-4o', 'GPT-4o-mini', 'Claude 3.5 Sonnet', 'Llama 3.1 70B', '本地模型 (Qwen2)'];\nlet selectedModel = 'GPT-4o';\n\n/**\n * 渲染聊天页面\n */\nexport function render(container) {\n  container.classList.add('chat-layout');\n  container.style.padding = '0';\n\n  container.innerHTML = `\n    <!-- 对话列表侧栏 -->\n    <div class=\"chat-sidebar\">\n      <div class=\"chat-sidebar-header\">\n        <h3>💬 对话列表</h3>\n        <button class=\"bt
<truncated 12641 bytes>